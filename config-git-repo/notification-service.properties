# --- Port et URL du service -------------------------------------------
notification-service.port=8083
notification-service.base-url=http://localhost:8083

# --- Metadonnees applicatives (lues par settings.py) ------------------
spring.application.name=notification-service
spring.cloud.discovery.enabled=true

# --- Securite Django --------------------------------------------------
django.secret_key=django-insecure-remote-secret-key-change-in-production

# --- Eureka (enregistrement du service Python) ------------------------
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.hostname=localhost

# --- Email SMTP -------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=boutheinabelg1@gmail.com
spring.mail.password=cfnf myax xiub iykm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
