admin:
  email: belghaouiaamal@gmail.com

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  cloud:
    config:
      override-system-properties: false
      uri: http://localhost:9999
  tracing:
    sampling:
      probability: 1.0
