spring:
  config:
    import: optional:configserver:http://localhost:9999
  application:
    name: gateway-service
management:
  tracing:
    enabled: false
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans" # Optionnel si désactivé
